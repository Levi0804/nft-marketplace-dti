export type Pokemon= {
    name: string;
    address: string;
  };
  
  export type ATraits = {
      count: number;
  };
  
  export type Listing = {
    listing_object_address: string;
    price: number;
    seller_address: string;
  };
  